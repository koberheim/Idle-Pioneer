## Autoload: static content registry (ResourceDef, RecipeDef, RegionDef, UpgradeDef, ...).
## Read-only after boot. Populated in task D4 - this is the skeleton only (task F4).
extends Node


func _ready() -> void:
	pass
